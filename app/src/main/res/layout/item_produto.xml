<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="4dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="#062DC8"
        android:padding="12dp">

        <!-- Foto do produto -->
        <ImageView
            android:id="@+id/imgProduto"
            android:layout_width="100dp"
            android:layout_height="140dp"
            android:scaleType="centerCrop"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- Nome do Produto -->
        <TextView
            android:id="@+id/txtNomeProduto"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:text="Nome do Produto"
            android:textColor="#DC0A0A"
            android:textSize="18sp"
            android:textStyle="bold"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toEndOf="@id/imgProduto"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- Preço do Produto -->
        <TextView
            android:id="@+id/txtPrecoProduto"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:text="R$ 0,00"
            android:textColor="#E4E5E9"
            android:textSize="20sp"
            android:textStyle="bold"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="@id/txtNomeProduto"
            app:layout_constraintTop_toBottomOf="@id/txtNomeProduto" />

        <!-- Descrição / Observação (Sem limitação de altura) -->
        <TextView
            android:id="@+id/txtObsProduto"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:layout_marginEnd="8dp"
            android:text="Descrição/Observação do produto..."
            android:textColor="#D4D7E2"
            android:textSize="18sp"
            android:textStyle="bold"
            app:layout_constraintEnd_toStartOf="@id/imgCarrinho"
            app:layout_constraintStart_toStartOf="@id/txtNomeProduto"
            app:layout_constraintTop_toBottomOf="@id/txtPrecoProduto" />

        <!-- Ícone do Carrinho (Fixo no canto inferior direito) -->
        <ImageView
            android:id="@+id/imgCarrinho"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:contentDescription="Carrinho de Compras"
            android:src="@android:drawable/ic_menu_add"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:tint="#FFFFFF" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</androidx.cardview.widget.CardView>