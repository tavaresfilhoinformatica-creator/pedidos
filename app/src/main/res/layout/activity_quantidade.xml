<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    style="@style/Widget.MaterialComponents.Button.OutlinedButton"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="24dp"
    android:gravity="center_horizontal">

    <!-- Nome do Produto Selecionado -->
    <TextView
        android:id="@+id/txtNomeProduto"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Nome do Produto"
        android:textSize="22sp"
        android:textStyle="bold"
        android:layout_marginTop="32dp"
        android:gravity="center" />

    <!-- Preço Unitário -->
    <TextView
        android:id="@+id/txtPrecoProduto"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="R$ 0,00"
        android:textSize="18sp"
        android:textColor="#666666"
        android:layout_marginTop="8dp" />

    <!-- Seletor de Quantidade (- 1 +) -->
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginTop="48dp">

        <Button
            android:id="@+id/btnMenos"
            android:layout_width="50dp"
            android:layout_height="50dp"
            android:text="-"
            android:textSize="20sp" />

        <TextView
            android:id="@+id/txtQuantidade"
            android:layout_width="60dp"
            android:layout_height="wrap_content"
            android:text="1"
            android:textSize="24sp"
            android:textStyle="bold"
            android:gravity="center"
            android:layout_marginHorizontal="16dp" />

        <Button
            android:id="@+id/btnMais"
            android:layout_width="50dp"
            android:layout_height="50dp"
            android:text="+"
            android:textSize="20sp" />
    </LinearLayout>

    <!-- Espaçador para empurrar os botões para o final -->
    <View
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

    <!-- Botão: Continuar Selecionando -->
    <Button
        android:id="@+id/btnContinuarSelecionando"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Continuar Selecionando"
        style="?attr/materialButtonOutlinedStyle"
        android:layout_marginBottom="12dp" />

    <!-- Botão: Finalizar Pedido -->
    <Button
        android:id="@+id/btnFinalizarPedido"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Finalizar Pedido" />

</LinearLayout>